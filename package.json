{
  "name": "couch-aws-logs",
  "version": "1.0.0-alpha",
  "description": "Feeds metrics from a CouchDB instance to AWS CloudWatch.",
  "main": "index.js",
  "bin": "bin.js",
  "scripts": {
    "test": "standard && dependency-check . --unused --no-dev && mocha -R spec test.js"
  },
  "author": "Diana Thayer <garbados@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.184.0",
    "request": "^2.83.0",
    "yargs": "^10.1.1"
  },
  "devDependencies": {
    "dependency-check": "^3.0.0",
    "mocha": "^5.0.0",
    "nyc": "^11.4.1",
    "standard": "^10.0.3"
  }
}
